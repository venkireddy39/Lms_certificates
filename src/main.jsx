import React from 'react'
import ReactDOM from 'react-dom/client'
import Certificates from './App'
import './Certificates.css'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <Certificates />
    </React.StrictMode>,
)
